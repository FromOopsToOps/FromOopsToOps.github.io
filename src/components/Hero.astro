---
import { siteConfig } from '../config.ts';
---

<section
  class="mono-mesh to-mono-lighter relative bg-gradient-to-b from-[#070A0F] px-4 pt-32 pb-20 sm:px-6 lg:px-8"
>
  <div class="bg-mono-black absolute top-32 left-0 h-32 w-1"></div>
  <div class="bg-mono-black absolute top-32 right-0 h-32 w-1"></div>

  <div class="mx-auto max-w-7xl">
    <div class="mx-auto max-w-4xl text-center">
      <h1
        class="text-white scroll-fade-up mb-6 text-5xl leading-tight font-extrabold sm:text-6xl lg:text-7xl"
      >
        {siteConfig.hero.headline}<br />
        <span class="text-white/80">{siteConfig.hero.subheadline}</span>
      </h1>
      <p
        class="text-white/80 scroll-fade-up mb-10 text-xl leading-relaxed sm:text-2xl"
      >
        {siteConfig.hero.description}
      </p>
      <div
        class="scroll-fade-up mb-16 flex flex-col justify-center gap-4 sm:flex-row"
      >
        <a
          href={siteConfig.hero.cta.primary.href}
          class="bg-mono-black rounded-lg px-8 py-4 font-semibold text-white shadow-lg"
        >
          {siteConfig.hero.cta.primary.text}
        </a>
        <a
          href={siteConfig.hero.cta.secondary.href}
          class="border-[color:var(--color-vault-neon)] text-white rounded-lg border-2 bg-[#070A0F] px-8 py-4 font-semibold"
        >
          {siteConfig.hero.cta.secondary.text}
        </a>
      </div>

      <div
        class="border-mono-lighter mx-auto grid max-w-3xl grid-cols-3 gap-8 border-t pt-20"
      >
        {
          siteConfig.hero.stats.map((stat, index) => (
            <div class={`float scroll-fade-up stagger-${index + 1}`}>
              <div class="text-white mb-2 text-4xl font-bold">
                {stat.value}
              </div>
              <div class="text-white/60 text-sm tracking-wide uppercase">
                {stat.label}
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>
