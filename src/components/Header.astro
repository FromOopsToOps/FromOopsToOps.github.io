---
const home = `${Astro.base}/`;
const anchor = (id) => `${home}#${id}`;
---

<header class="site-header">
  <div class="container header-inner">
    <a class="brand" href={home} aria-label="Odin's Vault home">
      <span class="brand-wordmark">ODIN’S VAULT</span>
      <span class="brand-tag">cyber vault</span>
    </a>

    <nav class="nav">
      <a class="navlink" href={anchor("features")}>Features</a>
      <a class="navlink" href={anchor("how")}>How it works</a>
      <a class="navlink" href={anchor("pricing")}>Services</a>
      <a class="navlink" href={anchor("faq")}>FAQ</a>
      <a class="navlink cta" href={anchor("contact")}>Get Started</a>
    </nav>
  </div>
</header>
